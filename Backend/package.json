{
  "name": "odoo-stockmaster-backend",
  "version": "1.0.0",
  "description": "Backend for StockMaster inventory management system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node prisma/seed.js",
    "migrate": "npx prisma migrate deploy",
    "generate": "npx prisma generate",
    "build": "npm install && npx prisma generate && npx prisma migrate deploy"
  },
  "keywords": ["inventory", "stock", "management"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.19.2",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.15"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "^6.1.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
